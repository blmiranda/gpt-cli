{
  "name": "gpt-cli",
  "version": "1.0.0",
  "description": "ChatGPT cli client",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "gpt": "./dist/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc"
  },
  "author": "Brian Miranda",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.9.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "openai": "^4.19.0"
  }
}
